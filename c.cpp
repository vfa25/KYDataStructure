#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

void change(int * j) {
  *j = 5;
}

int main()
{
  // int i = 0173;
  // // int i = 0x7b;
  // printf("a = %d\n", i);
  // float f = 1.234;
  // printf("console %f\n", f);
  // char c = 'a';
  // c -= 32;
  // printf("%c\n", c);
  // int i = 5;
  // float j = i / 2;
  // float k = (float)i / 2;
  // printf("j=%f, k=%f\n", j, k);
  // char c;
  // while (scanf("%c", &c) != EOF)
  // {
  //   if (c != '\n')
  //   {
  //     printf("%c", c - 32);
  //   } else {
  //     printf("\n");
  //   }
  // }
  // char c[6] = "hello";
  // char d[5] = "how";
  // printf("%s-----%s\n", c, d);
  // char e[20], f[20];
  
  
  // scanf("%s%s", e, f);
  // printf("%s----%s\n", e, f);
  // int i = 5;
  // int *p = &i;
  // printf("i=%d\n", i);
  // printf("p=%d\n", p);

  // printf("*p=%d", *p);

  // int i = 10;
  // printf("beforei=%d\n", i);
  // change(&i);
  // printf("afetr i=%d\n", i);

  // int a[5] = {2,7,8,4,5};
  // int *p;
  // int j;
  // p = a;
  // j = *p++;
  // printf("a[0]=%d, j=%d, *p=%d", a[0], j, *p); // 2 2 7
  // j = p[0]++;
  // printf("a[0]=%d, j=%d, *p=%d", a[0], j, *p); // 2 7 8
  // short i = 0x8011;
  // printf("j=%d\n", i);
  // int S1[3] = { -1, 0 , 9 };
  // int S2[4] = { -25, -10, 20, 11 };
  // int S3[5] = { 2,9, 17, 30, 41};
  
  // int i1 = 0, i2 = 0, i3 = 0;
  // int result = INT_MAX;
  
  //   int curDir = abs(S1[i1] - S2[i2]) + abs(S2[i2] - S3[i3]) + abs(S3[i3] - S1[i1]);
  //   if (curDir < result) {
  //     result = curDir;
  //   }
}
